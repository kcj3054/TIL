
## LeastJob 클래스
````
package com.example.item02.scheduler;

public class LeastJob implements Scheduler{
    @Override
    public void getNextCall() {
        System.out.println("상담 전화를 순서대로 대기열에서 가져옵니다");
    }

    @Override
    public void sendCallToAgent() {
        System.out.println("현재 상담업무가 없거나 상담대기가 가장 적은 상담원에게 할당합니다");
    }
}

````


## RoundRobin 클래스

````
package com.example.item02.scheduler;

public class RoundRobin implements Scheduler{
    @Override
    public void getNextCall() {
        System.out.println("상담 전화를 순서대로 대기열에서 가져옵니다");
    }

    @Override
    public void sendCallToAgent() {
        System.out.println("현재 상담업무가 없거나 상담대기가 가장 적은 상담원에게 할당합니다");
    }
}

````

## Scheduler 인터페이스

````
package com.example.item02.scheduler;

public interface Scheduler {

    void getNextCall();
    void sendCallToAgent();

}

````

## SchedulerTest 소스 
````
package com.example.item02.scheduler;

import java.io.IOException;


/***
 * jdbc는 인터페이스이다. 이것을 구현한 구현체들이 oracle, mysql..등이있다
 * 프로그래머들(클라이언트들)은 구현할 때 이러한 것들을 oracle이 구현한 jar파일을 까서 보지않고 interface의 이용 가능한 기능들만 보고 사용한다
 * 만약, 어떤 회사가 oracle을 쓰다가 mysql로 바꿔야할 때는 구현체만 바꾸면된다. 그렇기에  인터페이스를 사용하면 확장성이 좋다
 * 또한 클라이언트는 인터페이스에 정의된 메소드 명세만 보고 사용가능하다
 *interface -> 명세서,
 *
 *
 */

/***
 * ===============인터페이스 요소=============
 * 상수 : 모든 변수는 상수로 치환
 * 메서드 : 모든 메서드는 추상 메서드로 된다.
 * 디폴트 메서드 : 자바8부터 기본 구현을 가지고 있는 메서드가 있을 수 있고, 구현 클래스에서 재정의 가능
 * 정적 메서드 : 인스턴스 생성과 상관없이 인터페이스 타입으로 사용할 수 있는 메서드
 */
public class SchedulerTest {
    public static void main(String[] args) throws IOException {


        // R, L, P
        System.out.println("전화 상담 배분방식을 선택하세요, R, L, P");


        int ch = System.in.read();
        Scheduler scheduler = null;


        if(ch == 'R' || ch == 'r') {
            scheduler = new RoundRobin();
        } else if (ch == 'L' || ch == 'l') {
            scheduler = new LeastJob();
        } else if(ch == 'P' || ch == 'p') {
            scheduler = new PriorityAllocation();
        }

        //
        scheduler.getNextCall();
        scheduler.sendCallToAgent();
    }
}

````